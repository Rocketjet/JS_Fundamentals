/* function makeNegative(num) {
  if (num > 0) {
    return num - (num * 2);
  } else if (num == 0) {
    return 0;
  } else {
    return num;
  }
} */

/* function makeNegative(num) {
  return -Math.abs(num);
} */