/* const checkPerfectNumber = function (num) {
  debugger
  let divisors = 0;
  for (let i = 1; i <= num / 2; i++) {
    if (num % i === 0) {
      divisors += i;
    }
  }
  if (divisors == num) {
    return true;
  }
  return false;
};
 */